# Keep the workspace hoisting predictable so native deps resolve from package roots.
node-linker=isolated
strict-peer-dependencies=false
# node-pty (observed sessions, M5) is an optional native dep; never block install on it.
